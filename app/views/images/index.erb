<!DOCTYPE html>
<html>
<header>
</header>
<body>

<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<% if @session.key?(:id) then %>
<h2>Welcome <%= User.find(@session[:id]).username %>!</h2> 
<h3><a href="/images/new">Post new image</a> <a href="/logout">Logout</a></h3>
<% else %>
<h2>Please <a href="/signup">signup</a> or <a href="/login">login</a> to
post.</h2>
<% end %>

<ul>
<% Image.all.each do |i| %>
  <li><p><a href="/images/<%= i.id %>/draw"><%= i.name %></a></p>
  <p><%= i.src %></p>
  <p><a href="/users/<%= i.user.id %>"><%= i.user.username %></a></p>
  </li>
  
<% end %>
</ul>

</body>
</html>